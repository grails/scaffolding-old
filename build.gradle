apply plugin:'idea'

subprojects {

    group "org.grails"
    version "2.0.1.BUILD-SNAPSHOT"

    repositories {
        mavenLocal()
        mavenCentral()
        maven { url "https://repo.grails.org/grails/core" }
    }

}