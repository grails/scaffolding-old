apply plugin:'idea'

subprojects {

    group "org.grails"
    version "2.1.1.BUILD-SNAPSHOT"

    repositories {
        mavenLocal()
        mavenCentral()
        maven { url "https://repo.grails.org/grails/core" }
    }

}