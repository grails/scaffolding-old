apply plugin:'idea'

subprojects {

    group "org.grails"
    version "2.0.0.RC1"

    repositories {
        mavenLocal()
        mavenCentral()
        maven { url "https://repo.grails.org/grails/core" }
    }

}